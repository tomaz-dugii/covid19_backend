{
  "name": "covid19s",
  "version": "1.0.0",
  "description": "covid19 RestAPI",
  "main": "dist/covid19.js",
  "scripts": {
    "build": "tsc --strict",
    "prestart": "npm run build",
    "watch": "./node_modules/.bin/nodemon -V -w . --ext \".ts\" --exec \"npm run start\"",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.7",
    "@types/node": "^14.0.22",
    "nodemon": "^2.0.4",
    "tslint": "^6.1.2",
    "typescript": "^3.9.6"
  },
  "dependencies": {
    "express": "^4.17.1",
    "pg-promise": "^10.5.8"
  }
}
